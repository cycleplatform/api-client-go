{
    "generator": {
        "ignore_not_implemented": [
            "sum types with same names",
            "object with additionalProperties",
            "discriminator inference",
            "complex defaults"
        ],
        "features": {
            "disable": ["paths/server", "ogen/otel"]
        }
    }
}
